<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             xmlns:vm="clr-namespace:Test.PrismMaui.ViewModels.Regions"
             x:Class="Test.PrismMaui.Views.Regions.SomeRegionView"
             x:DataType="vm:SomeRegionViewModel">
  <VerticalStackLayout>
    <lvc:CartesianChart x:Name="CounterChart"
                        BackgroundColor="Wheat"
                        LegendBackgroundPaint="{Binding LegendTextPaint}"
                        LegendPosition="Bottom"
                        LegendTextPaint="{Binding LegendTextPaint}"
                        LegendTextSize="16"
                        Series="{Binding CounterSeries}"
                        XAxes="{Binding AxesX}"
                        YAxes="{Binding AxisY}" />
  </VerticalStackLayout>
</ContentView>
