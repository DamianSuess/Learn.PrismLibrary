<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="Test.PrismXF.Views.RootMasterDetailPage">

  <!-- Sidebar -->
  <MasterDetailPage.Master>
    <ContentPage Title="Menu">
      <StackLayout Padding="20">
        <!-- TODO: // Update the Layout and add some real menu items  -->
        <Button Text="Main Page"
                Command="{Binding NavigateCommand}"
                CommandParameter="NavigationPage/MainPage" />

        <Button Text="2nd Page"
                Command="{Binding NavigateCommand}"
                CommandParameter="NavigationPage/SecondPage" />

        <Button Text="3rd Page"
                Command="{Binding NavigateCommand}"
                CommandParameter="NavigationPage/ThirdPage" />
      </StackLayout>
    </ContentPage>
  </MasterDetailPage.Master>
</MasterDetailPage>